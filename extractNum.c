int extractNum(char* nums, int i) {
    // input like "10 20 30" or "10\n20\n30" and i gets the ith number
    // returns -1 if i is out of bounds
    int num = 0;
    int count = 0;
    int j = 0;
    while(nums[j] != "\n") {
        
    }
    return num;
}