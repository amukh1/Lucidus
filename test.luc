dec printf(tmp:char*, ...) -> int;

struct Car {
    name: char*,
    year: int,
    price: int
};

dec main() -> int;
def main() -> int {
    printf("Hello World\n");
    printf("Programming my %dth \'compiler\'\n", 3);
    printf("After ~%d years of programming..\n", 4);
    x:int := 0;
    y:int* := ptr x;
    z:int := *y;
    abc:Car;
    abc.name; 
    // abc.name = ...; // invalid.
    return x;
}
