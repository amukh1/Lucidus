dec printf(tmp:char*, ...) -> int;
dec malloc(size:int) -> int*;

dec loop(n:int) -> bool;

dec main() -> int;
def main() -> int {
    loop(16);
    return 0;
}

def loop(n:int) -> bool {
    if(n == 16) -> {
        printf("equal!");
    }
    return (n == 16);
}