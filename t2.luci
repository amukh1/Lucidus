dec printf(tmp:char*, ...) -> int;
dec malloc(size:int) -> int*;

dec loop(n:int) -> int*;

dec main() -> int;
def main() -> int {
    loop(16);
    return 0;
}

def loop(n:int) -> int* {
    goto h;
    
    label h:
    
    printf("loop\n");

    goto h;
    x := 4;
    y := ptr x;
    return y;
}