import "include.luci"
// my sufferings

struct Graph {
    nodesc: int,
    edgesc: int,
    edges: int**
};

def main() -> int {
    x:int = 5;
    y:int* = (ptr x);

    printf("x = %d\n", x);
    printf("*y = %d\n", *y);

    *y = 10;

    printf("x = %d\n", x);

    v:int* = malloc(8)->(int*);
    *v = 10;

    G:Graph = malloc(24)->(Graph);
    // (ptr (G.nodesc)) = 5;
    G.nodesc = 5;

    printf("G.nodesc = %d\n",  (G.nodesc));
    return 0;
}