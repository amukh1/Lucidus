import "include.luci"
import "structures.luci"
// my sufferings

def main() -> int {
    i:int = 0;
    while(i != 10) -> {
        printf("Hello, World! %d!\n", i);
        (ptr i) = i + 1;
    }
    printf("Hello! sizeof int = %d\n", sizeof int);
    printf("%d\n", 1+-(1+1));
    return 0;
}