import "include.luci" // import luci file that includes useful declarations.

dec main() -> int;
def main() -> int {
    x:int = 4;
    y:int = 4;
    printf("%d\n", x);
    if(x == y) -> {
        printf("x == y\n");
        while(1 == 1) -> {
            printf("in while\n");
            if(x == 4) -> {
                printf("x == 4\n");
            }
        }
    }
    return 0;
}